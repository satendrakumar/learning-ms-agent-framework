[project]
name = "learning-ms-agent-framework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "agent-framework>=1.11.0",
]

[dependency-groups]
dev = [
    "python-pptx>=1.0.2",
]
